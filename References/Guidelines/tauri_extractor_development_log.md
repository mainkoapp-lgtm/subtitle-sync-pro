# Tauri 자막 추출기 개발 및 트러블슈팅 기록

### 2026-05-06: PGS 자막 OCR 파이프라인 안정화 및 0-byte SRT 오류 해결 (최종)
- **문제점**: Tesseract 경로 인식 실패(Windows UNC `\\?\` 이슈), 언어 데이터 누락, 타임라인 매칭 불일치로 인한 0-byte 또는 부정확한 SRT 생성.
- **수정 과정 및 핵심 코드**:
    1. **Tesseract 경로 정규화**: `replace("\\\\?\\", "")`를 통해 윈도우 특수 경로 이슈 해결.
    2. **환경 변수 강제 주입**: `TESSDATA_PREFIX`를 Tesseract 실행 시점에 동적 설정.
    3. **정밀 타임라인 매칭**: `ffprobe` 패킷 타임라인과 추출 이미지(PNG) 간의 **비율 매칭(Proportional Matching)** 및 **동일 대사 그룹화(Grouping)** 로직 구현.
    4. **무삭제 추출 알고리즘**: 글자가 있는 모든 프레임을 훑고 중복을 제거하여 1,000줄 이상의 방대한 자막도 유실 없이 추출 가능하게 개선.
- **결과**: **[성공]** (아담스 패밀리 4K 블루라이 기준 838개 자막 완벽 추출 확인)

---

## 2026-04-22: 자막 추출 로직 고도화 및 네트워크 드라이브 이슈 해결

- **문제점**:
    1. **저장 실패**: 무확장자 임시 파일(.tmp) 사용 시 FFmpeg가 출력 형식을 인식하지 못해 `Invalid argument` 에러 발생.
    2. **진행률(%) 미표시**: 네트워크 드라이브에서 `ffprobe` 호출 시 지연/프리징 발생 및 `stderr` 파싱의 불안정성으로 인해 진행률이 0%에 고정됨.
    3. **중단 시 응답 없음 (ANR)**: 동기식(Sync) I/O 및 프로세스 대기 로직으로 인해 네트워크 지연 시 UI 스레드와 백엔드 통신이 차단됨. 또한 `taskkill /T` 옵션이 상위 프로세스에 영향을 주어 앱이 강제 종료됨.

- **수정 과정 및 핵심 코드**:
    1. **비동기화 (Async/Tokio)**: 모든 Tauri 커맨드를 `async`로 전환하고, `tokio::process::Command` 및 비동기 I/O(`BufReader`, `lines()`)를 사용하여 프리징 방지.
    2. **정밀 파싱 (-progress pipe:1)**: 사람이 읽는 로그 대신 기계용 진행률 옵션을 도입하여 `out_time_us`를 파싱. `stderr`에서는 별도 비동기 태스크로 `Duration`을 추출하여 취합.
    3. **안전한 종료**: `taskkill`에서 트리 종료(`/T`) 옵션을 제거하고 개별 PID만 종료하여 시스템 안정성 확보.
    4. **무결성 검사**: `fs::copy` 등 파일 작업의 에러를 무시하지 않고 프런트엔드까지 전달하도록 강화.
    5. **이름 중복 회피**: 동일 파일명 존재 시 `(2)`, `(3)` 접미사를 자동으로 추가하여 데이터 유실 방지.

- **결과**: [성공] (2026-04-22 모든 기능 검증 완료)

---

### 2026-04-22: 배너 광고 클릭 링크(네이버) 추가 및 서버 연동 최적화
- **문제점**: 하단 배너(FlowStateTimer)가 클릭이 불가능하고 링크가 연결되어 있지 않음. 또한 앱 내부에 iframe 주소가 하드코딩되어 있어 서버에서 제어가 어려움.
- **수정 과정 및 핵심 코드**: 
  1. `img/flowstatetimer/index.html`: `banner-container`에 `onclick` 이벤트를 추가하여 `https://www.naver.com`으로 연결되도록 수정.
  2. `tools/tauri_extractor/src/App.tsx`: 하드코딩된 `iframe` 주소를 서버 설정(`config.ad_config.target_url`)에서 불러오도록 수정 제안.
- **결과**: [성공] (2026-04-22 클릭 링크 연동 확인)

---

### 2026-04-22: 서버 기반 HTML 배너(FlowStateTimer) 노출 이슈 해결
- **문제점**: 서버에 새 `index.html` 배너를 업로드했음에도 불구하고, 프로그램에서 이전의 정적 이미지 배너가 계속 출력됨. 원인은 프로그램 로직이 `image_url`이 존재할 경우 이미지를 우선적으로 렌더링하고, HTML 배너 주소를 받아올 수 있는 규격이 없었기 때문임.
- **수정 과정 및 핵심 코드**:
  1. `App.tsx`: `RemoteConfig` 인터페이스에 `html_url` 필드를 추가하고, 렌더링 로직에서 `html_url`이 있을 경우 `iframe`으로 서버의 HTML 배너를 우선적으로 출력하도록 수정.
  2. `server/public/latest_version.json`: `ad_config` 내에 `html_url`을 추가하여 업로드된 `index.html`을 가리키도록 설정.
- **결과**: [성공] (2026-04-22 Firebase 배포 및 프로그램 연동 확인 완료)

---

### 2026-04-22: 배너 클릭 이동 이슈 및 샌드박스 정책 적용
- **문제점**: 서버 기반 HTML 배너 노출에는 성공했으나, 배너 클릭 시 외부 브라우저가 열리지 않는 현상 발생. 
- **원인 분석**:
  1. **Tauri iframe 보안**: WebView2 환경에서 iframe 내부의 `window.open` 및 `target="_blank"`는 부모 윈도우의 허용 없이는 자동으로 차단됨.
  2. **플러그인 격리**: iframe 내부는 원격 URL(Firebase)에서 로드되므로 Tauri API(`@tauri-apps/plugin-opener`)를 직접 호출할 수 없음.
- **수정 과정 및 핵심 코드**:
  1. **샌드박스 속성 적용**: `App.tsx`의 iframe 태그에 `sandbox="allow-scripts allow-popups allow-popups-to-escape-sandbox allow-same-origin"`을 추가하여 팝업 차단 정책을 완화함.
  2. **표준 a 태그 전환**: `onclick` 방식에서 `<a>` 태그 기반의 표준 링크 구조로 수정하여 WebView의 링크 가로채기 호환성을 높임.
  3. **서버 동기화**: 수정된 HTML 형식을 Firebase Hosting에 최종 배포 완료.
- **결과**: [성공] (배너 노출 및 서버 연동 완료 / 클릭 이슈는 기술적 제한 사항으로 기록)

---

### 2026-04-22: 메시지 브릿지(postMessage)를 이용한 클릭 이동 시도 및 기술적 한계 기록
- **문제점**: 샌드박스 정책 완화 및 표준 `<a>` 태그 전환 후에도 데스크톱 앱 내의 `iframe` 배너 클릭 시 외부 브라우저가 열리지 않음. 
- **해결 시도**:
  1. **메시지 브릿지 구현**: `iframe` 내에서 직접 창을 열지 않고, `window.parent.postMessage`를 통해 부모 앱(React)에 URL 정보를 전달하는 방식 도입.
  2. **부모 앱 리스너 추가**: `App.tsx`에서 메시지를 수신하여 부모 컨텍스트에서 `window.open`을 대치 호출하는 브릿지 로직 구현.
- **결과**: [실패] (일부 데스크톱 앱 보안 환경에 따라 원격 iframe과의 통신 또는 팝업 제어가 여전히 차단됨. 향후 원격 `iframe` 대신 로컬 컴포넌트 방식이나 Tauri 네이티브 쉘 핸들링으로의 전환 검토 필요)

---

### 2026-04-22: Tauri opener 플러그인 임포트 오류 수정 및 화이트스크린 해결
- **문제점**: 
  1. `App.tsx`에서 `@tauri-apps/plugin-opener`로부터 `open`을 임포트하려 했으나, Tauri 2.0에서는 `openUrl`로 익스포트되어 있어 임포트 오류 발생. 이로 인해 앱 실행 시 화이트스크린(White Screen) 현상 발생.
  2. `Track` 인터페이스에 `is_text_based` 속성이 누락되어 빌드 에러 발생.
- **수정 과정 및 핵심 코드**:
  1. `App.tsx`: 임포트문을 `import { openUrl } from "@tauri-apps/plugin-opener";`로 수정.
  2. `Track` 인터페이스에 `is_text_based?: boolean;` 추가.
  3. 미사용 아이콘(`Globe`, `Link`) 임포트 제거하여 코드 최적화.
- **결과**: [성공] (2026-04-22 빌드 오류 및 화이트스크린 해결 완료)

---

### 2026-04-22: iframe 샌드박스 한계 극복을 위한 투명 오버레이 적용
- **문제점**: Tauri 앱에서 원격 `iframe`을 사용할 경우, 도메인이 다르기 때문에(Cross-Origin) `postMessage`를 이용한 클릭 이벤트 전달이 보안 정책에 의해 차단되거나 신뢰성 있게 작동하지 않음. 이로 인해 `openUrl`을 도입했음에도 브라우저가 열리지 않는 현상 지속.
- **해결 방법 및 핵심 코드**:
  1. **Tauri Opener 필수 사용**: Tauri 2.0에서는 `window.open`이 시스템 브라우저를 열지 못하므로 `@tauri-apps/plugin-opener`의 `openUrl`을 사용해야 함.
  2. **투명 오버레이 기법**: `iframe` 내부의 클릭 이벤트에 의존하지 않고, 부모(React) 레이어에서 `iframe` 위에 `z-index`가 높은 투명한 `div`를 덮어씌움.
  3. **이벤트 가로채기**: 사용자가 배너를 클릭할 때 `iframe` 대신 오버레이가 클릭을 수신하게 하여, 부모 컨텍스트에서 직접 `openUrl`을 호출함으로써 보안 제약을 완전히 우회함.
- **결과**: [성공] (2026-04-22 배너 클릭 및 외부 브라우저 연동 성공 확인)

---

### 2026-04-22: 클릭몬 광고 통합 및 수익 최적화
- **문제점**: 동일 IP 반복 작업 시 Monetag 수익 누락 우려 및 수익원 단일화 문제 해결 필요.
- **수정 과정 및 핵심 코드**: 
    - `ClickmonBanner.tsx` 컴포넌트 신설: `document.writeln` 방식의 클릭몬 160x600 세로 배너를 React에서 안전하게 로드하기 위해 iframe 샌드박스 구조로 구현.
    - `App.tsx` 사이드바 수정: 좌측은 쿠팡 파트너스, 우측은 클릭몬 배너가 대칭으로 노출되도록 `AdSidebar` 컴포넌트 업데이트.
    - `handleSync` 로직 고도화: `싱크 맞추기` 버튼 클릭 시 Monetag와 함께 클릭몬 전면 광고(팝업)가 트리거되도록 `triggerClickmonPopup` 함수 구현 및 적용.
- **결과**: [테스트 필요] (로컬 코드 수정 완료, 추후 배포 및 실 연동 테스트 예정)

---

### 2026-04-22: 자막 추출기(EXE) 배포 빌드 및 보안 최적화 적용
- **문제점**: 배포용 빌드(Release) 시 소스 코드 역공학(리버스 엔지니어링) 방지, 프런트엔드 소스 맵 노출 방지 및 `tauri.conf.json`의 `identifier` 명명 규칙(언더스코어 불가) 위반으로 인한 빌드 에러 발생.
- **수정 과정 및 핵심 코드**:
  1. **빌드 오류 수정**: `tauri.conf.json`의 `identifier`를 `com.desktop.toolstauri_extractor`에서 언더스코어를 하이픈으로 변경하여 `com.desktop.toolstauri-extractor`로 수정.
  2. **프런트엔드 난독화 (Vite)**: `vite.config.ts` 파일에 `build: { sourcemap: false }`를 명시적으로 추가하여 배포 시 소스 맵 생성을 차단.
  3. **백엔드(Rust) 심볼 제거 및 최적화**: `src-tauri/Cargo.toml` 파일에 `[profile.release]` 블록을 신설하여 `strip = true` (심볼 제거), `opt-level = "z"` (사이즈 최적화), `lto = true` (링크 타임 최적화) 등을 적용하여 역공학을 어렵게 하고 실행 파일 크기를 최소화.
  4. **백업 수행**: 수정 전 코드를 `backup/20260422075900` 폴더에 복사본으로 저장 완료.
- **결과**: [테스트 필요] (현재 `npm run tauri build` 백그라운드 빌드 진행 중)

---

### 2026-04-22: 공식 빌드명 변경 ('SubFast Extractor')
- **문제점**: 배포 시 프로그램의 정체성을 명확히 하기 위해 기존의 임시 프로젝트명(`toolstauri_extractor`) 대신 공식 서비스 명칭 적용 필요.
- **수정 과정 및 핵심 코드**:
  1. `tauri.conf.json`: `productName`을 `"SubFast Extractor"`로 변경. (향후 빌드 시 해당 이름의 EXE 생성 예정)
  2. 규칙 및 정책 업데이트: `.agent/rules/terminology.md` 및 `D:\Project Temporary\정책\communication_terminology.md`에 공식 명칭 기록 완료.
- **결과**: [성공] (설정 및 규칙 반영 완료)

---

### 2026-04-22: 홈페이지 자막 추출기 다운로드 버튼 추가 및 가이드 문구 수정
- **문제점**: 
  1. 홈페이지에서 'SubFast Extractor'(자막 추출기)를 다운로드할 수 있는 공식 경로가 없음.
  2. 기존 '사용 방법' 문구가 명확하지 않아 사용자의 혼동 우려가 있음.
  3. 다운로드 버튼 클릭 시 수익화 모델(광고)이 적용되어 있지 않음.
- **수정 과정 및 핵심 코드**:
  1. **i18n.ts**: 
     - `downloadApp` 키를 추가하여 다국어 대응 (ko, en, ja, zh, hi).
     - 한국어 `guideStep1`, `guideStep2` 문구를 사용자의 요청에 맞춰 "싱크가 맞는 원본 자막은 '참조 자막'에 업로드하세요." 등으로 정밀 수정.
  2. **App.tsx**: 
     - 헤더 영역(`header-actions`)에 `Download` 아이콘을 포함한 '자막추출기 다운로드 (Windows)' 버튼 추가.
     - `handleDownloadApp` 비동기 함수 구현: 운영 모드(`isProduction`)일 때 Monetag 전면 광고(`show_10906696`)를 트리거한 후 드롭박스 다운로드 링크를 열도록 로직 설계.
  3. **App.css**: 
     - `.download-app-btn` 클래스에 프리미엄 그라데이션(`linear-gradient`)과 그림자 효과를 적용하여 가독성과 클릭률 향상.
- **결과**: [테스트 필요]

---

### 2026-04-22: 광고 수익 극대화를 위한 다이렉트 링크 로테이션(11종) 도입
- **문제점**: 단일 광고 링크 사용 시 동일 IP 반복 클릭에 대한 수익 인정률이 낮아질 우려가 있음.
- **수정 과정 및 핵심 코드**:
  1. **광고 풀(Pool) 구축**: 사용자가 생성한 11개의 Monetag 다이렉트 링크(`omg10.com`)를 `MONETAG_AD_LINKS` 배열로 통합 관리.
  2. **랜덤 셔플 로직**: `Math.random()` 기반의 `getRandomAdLink` 함수를 구현하여 클릭 시마다 11개 링크 중 하나를 무작위로 호출.
  3. **전방위 적용**: 
     - '자막 추출기 다운로드' 버튼 클릭 시 즉시 로테이션 광고 실행.
     - '싱크 맞추기' 광고 모달 내 실행 버튼 클릭 시 기존 광고와 병행하여 로테이션 광고 노출 (수익 이중화).
- **결과**: [성공] (동일 IP 반복 유입 시 수익 누락 최소화 및 광고 도배 방지 효과 기대)

---

### 2026-04-22: Monetag 다이렉트 링크 정책 확인 및 보안 가이드 수립
- **확인된 정책**: 
  1. **개수 제한 없음**: 계정당 생성 가능한 다이렉트 링크 수에 제한이 없음을 공식 확인.
  2. **다중 사용 허용**: 단일 웹사이트 내에서 여러 개의 서로 다른 다이렉트 링크를 동시에 사용하는 행위는 규정 위반이 아니며, 오히려 성과 분석을 위해 권장되는 방식임을 확인.
- **적용 결과**: 홈페이지 내 '싱크 맞추기'와 '추출기 다운로드' 버튼에 각각 독립적인 광고 링크를 배정하여 수익 분석 정밀도 향상.
- **관련 문서**: `References/Policies/monetag_policy.md` 및 `D:\Project Temporary\정책\monetag_policy.md`에 상세 내용 기록 완료.

---

### 2026-04-22: 홈페이지 헤더 버튼 레이아웃 수정 (한국어 사용자 친화적 배열)
- **문제점**: 홈페이지 헤더의 버튼 순서가 [다운로드 - 언어선택 - 사용방법]으로 되어 있어, 국내 사용자의 일반적인 시선 흐름 및 조작 편의성에 부적합함.
- **수정 과정 및 핵심 코드**:
  1. **App.tsx**: `header-actions` 내의 컴포넌트 순서를 사용자 요청에 따라 **[자막추출기 다운로드] -> [사용 방법] -> [한국어(언어 선택)]** 순으로 재배치.
  2. **레이아웃 유지**: 기존 Flexbox 기반의 `gap: 12px` 및 중앙 정렬 스타일을 유지하여 디자인 일관성 확보.
- **결과**: [성공] (서버 배포 완료)

---

### 2026-04-22: 홈페이지 가이드 모달 UI 개선 및 v0.12 릴리스
- **작업 내용**: 
  1. 가이드 모달에 탭 기능(홈페이지 사용법 / 자막 추출기 사용법) 도입.
  2. 탭 전환 시 상단 위치 고정을 위해 모달 최소 높이(`min-height: 620px`) 설정 및 Flex 레이아웃 적용.
  3. 자막 추출기 설명 구체화 (하드서브 불가 안내 포함) 및 드래그 미지원 문구 삭제.
  4. 홈페이지 버전을 `v0.12`로 상향.
- **결과**: [배포 완료] (GitHub Push를 통한 서버 자동 배포 파이프라인 가동 완료)

### 2026-04-22: [다국어 지원(i18n) 적용 누락 문제 해결]
- **문제점**: 홈페이지의 언어 변경 시, 모달 창(문의하기, 개인정보처리방침, 면책 조항 등), 푸터 텍스트 및 토스트 메시지 내의 문구들이 번역되지 않고 하드코딩된 한국어로 표시되는 문제.
- **수정 과정 및 핵심 코드**: 
  1. `frontend/src/i18n.ts`: 각 언어(ko, en, ja, zh, hi) 딕셔너리에 모달 타이틀, 설명, 면책조항, 버튼명, 토스트 알림 메시지용 30여개의 새로운 키값 추가.
  2. `frontend/src/App.tsx`: 하드코딩된 한국어 텍스트(예: `<h2>개인정보처리방침</h2>`)를 `t('privacyTitle')`과 같은 i18n 함수로 교체하여, 다국어가 즉각 반영되도록 수정.

### 2026-04-22: [다국어 지원(i18n) 적용 누락 문제 해결]
- **문제점**: 홈페이지의 언어 변경 시, 모달 창(문의하기, 개인정보처리방침, 면책 조항 등), 푸터 텍스트 및 토스트 메시지 내의 문구들이 번역되지 않고 하드코딩된 한국어로 표시되는 문제.
- **수정 과정 및 핵심 코드**: 
  1. `frontend/src/i18n.ts`: 각 언어(ko, en, ja, zh, hi) 딕셔너리에 모달 타이틀, 설명, 면책조항, 버튼명, 토스트 알림 메시지용 30여개의 새로운 키값 추가.
  2. `frontend/src/App.tsx`: 하드코딩된 한국어 텍스트(예: `<h2>개인정보처리방침</h2>`)를 `t('privacyTitle')`과 같은 i18n 함수로 교체하여, 다국어가 즉각 반영되도록 수정.
- **결과**: [성공]

### 2026-04-22: [자동 국가별 언어 감지 기능 검증]
- **내용**: VPN을 통한 국가 변경 시 자동 언어 전환 기능 작동 여부 확인.
- **검증 결과**: 
  - 일반 탭에서는 `sessionStorage`에 기록된 기존 방문 정보(`tracked_visit`)로 인해 즉각적인 언어 변경이 반영되지 않음(의도된 동작).
  - 시크릿 탭(새 세션)을 통해 접속 시, 접속 국가(예: 일본)에 맞는 언어(일본어)로 자동 전환됨을 최종 확인.
- **결과**: [성공]

---

### 2026-04-22: 광고 로드 및 동기화 런타임 에러(화이트스크린) 관련 버그 수정
- **문제점**:
  1. 우측 클릭몬 배너 광고가 렌더링되지 않음 (`<it>` 태그 오타 문제).
  2. 자막 추출기 다운로드 버튼 클릭 시 광고가 정상적으로 트리거되지 않음.
  3. '싱크 맞추기' 버튼 클릭 시 화면이 하얗게 변하는 현상 발생 (React 내에서 `document.writeln`이 호출되어 HTML 문서 구조가 리셋됨).
- **수정 과정 및 핵심 코드**:
  1. `ClickmonBanner.tsx`: `adScript` 내에서 잘못 구성된 `n='<' + 'i' + 't' ...` 문자열을 정상적인 `<script>` 태그를 생성하도록 수정.
  2. `App.tsx` (`handleDownloadApp`): `isProduction` 분기와 무관하게 우선적으로 새 창(`window.open`)에 랜덤 광고를 띄우고, 다운로드 파일은 팝업 차단을 방지하기 위해 현재 창(`window.location.href`)에서 실행하도록 로직 개선.
  3. `App.tsx` (`triggerClickmonPopup`): `document.writeln`을 포함하는 텍스트 스크립트 주입 방식을 폐기하고, `document.createElement('script')`에 클릭몬 팝업 `src` 속성만을 할당한 후 `appendChild`를 통해 DOM에 비동기 삽입하여 렌더링 파괴 방지.
- **결과**: [테스트 필요]

---

### 2026-04-22: [광고/수익화] 백엔드 주도형 4중 로테이션 및 동적 정책 준수 시스템 구축
- **문제점**: 
  1. 프론트엔드에 하드코딩된 광고 링크(Monetag 11개)로 인한 관리 비효율 및 수익 로직 분산.
  2. 쿠팡 파트너스 API 연동 시 시크릿 키 노출 위험 및 법적 대가성 문구(공정위) 누락에 따른 정책 위반 리스크.
  3. 다른 광고 시청 시에도 쿠팡 안내가 나오는 UX 불일치.
- **수정 과정 및 핵심 코드**:
  1. **백엔드 고도화 (`main.py`, `ad_providers.json`)**: 
     - Monetag(60%), 클릭몬(20%), 쿠팡(10%), FlowState(10%) 가중치 기반 로테이션 엔진 구현.
     - 쿠팡 API 실시간 호출 로직(HMAC 서명 방식)을 백엔드로 격리하여 보안 강화.
     - **의존성 업데이트**: `requirements.txt`에 쿠팡 API 서명을 위한 `pycryptodome` 패키지 추가.
  2. **프론트엔드 동적 대응 (`App.tsx`, `i18n.ts`)**:
     - **Pre-fetch 시스템**: 모달이 열리는 시점에 백엔드에서 광고 정보를 미리 가져와 타입을 확인.
     - **조건부 렌더링**: 광고 타입이 `coupang`인 경우에만 법적 대가성 문구와 쿠팡 전용 UI(빨간색 버튼)를 노출하도록 구현하여 정책 준수 및 UX 해결.
     - 하드코딩된 광고 배열을 완전히 제거하고 모든 제어권을 백엔드 API(`/api/reward/link`)로 이관.
- **결과**: [테스트 필요]

---

### 2026-04-22: [UX 최적화] 무의미한 5초 광고 대기 로직 삭제 및 버튼 즉시 활성화
- **문제점**: 
  1. 싱크 맞추기 클릭 시 나오는 모달에 5초 카운트다운이 있으나, 카운트다운 종료 후 브라우저 팝업 차단 정책으로 인해 광고가 자동 실행되지 않아 사용자 혼란 야기.
  2. 불필요한 대기 시간으로 인한 UX 저하 및 사용자의 이탈 유발 리스크.
- **수정 과정 및 핵심 코드**:
  1. **`App.tsx` 리팩토링 (`handleSync`)**:
     - `adCountdown` 상태 및 `setInterval` 카운트다운 로직 완전 삭제.
     - 기존에 브라우저 차단 우려가 있던 구형 Monetag Vignette 강제 자동 실행 로직(`monetagShowAd`)을 완전히 들어내고, 새롭게 구축된 백엔드 로테이션(Pre-fetch) 방식에 100% 의존하도록 단순화.
  2. **모달 UI 개선**:
     - 광고 정보를 받아오는 약 0.5초 동안만 로딩 상태를 보여주고, 완료 즉시 사용자가 클릭할 수 있는 버튼을 활성화시켜 직관성 향상 및 광고 노출 성공률 보장.
  3. **헤더 UI 단순화**: 자동 언어 감지 기능 안정화에 따라 불필요해진 수동 언어 선택(Dropdown) UI 제거.
- **결과**: [테스트 필요] (v0.13 업데이트 완료)

---

### 2026-04-22: [버그 수정] 자막 저장 시 원본 확장자 대신 .srt 강제 적용
- **문제점**: SMI 파일을 업로드하여 싱크를 맞춘 경우, 저장 시 확장자가 원본을 따라 .smi로 지정됨. 내부 데이터는 SRT 형식이므로 플레이어 재생 시 포맷 불일치 문제 발생 가능성.
- **수정 과정 및 핵심 코드**:
  - `App.tsx` (`handleDownload`): `targetFile?.name`에서 확장자를 분리하고 무조건 `.srt`를 붙이도록 수정하여 내용과 파일 형식의 정합성 확보.
- **결과**: [테스트 필요] (서버 재배포 진행)

---

### 2026-05-06: 인프라 데이터 트래킹을 위한 버튼 클릭 수집 기능 도입
- **문제점**: 광고 노출 대비 실제 사용자의 버튼 클릭(싱크 맞추기, 다운로드 등) 횟수를 정밀하게 파악하여 수익 모델 및 인프라 비용(API 토큰 등)을 최적화할 지표가 부족함.
- **수정 과정 및 핵심 코드**:
    1. `frontend/src/App.tsx` 내 `trackClick` 비동기 함수 구현: `axios.post('/api/collect-click')`을 통해 서버로 버튼 ID 전달.
    2. 주요 액션 버튼(`handleSync`, `handleDownloadApp`)에 클릭 추적 로직 삽입.
    3. `try-catch` 및 비동기 처리를 통해 수집 서버 장애 시에도 사용자 앱 경험(광고 실행 등)에는 영향이 없도록 설계.
- **결과**: [성공]

---

### 2026-05-06: 광고 차단기(AdGuard 등) 우회 문제 및 타이머 맹점 해결 (v0.14)
- **문제점**: 사용자가 광고 차단기 경고를 보고 새로고침한 뒤, 모달에서 '광고 시청 및 시작'을 누르면 광고 새 창이 차단됨에도 불구하고 2초 타이머가 그대로 흘러가 검증 토큰이 발급되고 싱크/다운로드가 진행되는(우회되는) 문제 발생.
- **수정 과정 및 핵심 코드**: 
    1. `frontend/src/App.tsx` 내 `checkAdBlocker`: `pagead2` 뿐만 아니라 실제 광고 제공 URL(`omg10.com`, `clickmon.co.kr` 등)로 직접 핑을 보내고, 차단 시 발생하는 네트워크 에러(`TypeError`)를 잡아내도록 검증 로직 대폭 강화.
    2. `frontend/src/App.tsx` 내 `setTimeout` 블록 수정: 2.5초 대기 후 곧바로 보상 토큰을 발급받기 전에 `checkAdBlocker()`를 한 번 더 비동기 호출. 새 창이 차단당해 광고가 로드되지 못하면 즉시 프로세스를 취소하고 경고 메시지를 출력하도록 보안 패치.
    3. 버전 상향: `v0.13` -> `v0.14` 로 업데이트.
- **결과**: [성공]

---

### 2026-05-06: 강력한 광고 차단기 대응을 위한 이미지 로드 검증 도입 (v0.15)
- **문제점**: 
    1. AdGuard 등 시스템 레벨 차단기가 `fetch` 요청을 가로채서 가짜 성공 응답을 보내는 경우, 기존 네트워크 핑 방식으로는 차단 여부를 100% 감별하기 어려움.
    2. 페이지 전체가 차단되는 경우 새 창에서의 상태를 메인 창에서 인지할 수 있는 추가 신호 필요.
- **수정 과정 및 핵심 코드**:
    1. **이미지 로드 테스트 (`checkAdBlocker`)**: `fetch` 대신 `new Image()` 객체를 생성하여 광고 도메인의 파비콘 등을 로드 시도. `onerror` 발생 시 즉각 차단으로 판정하는 로직 추가. 이는 브라우저의 리소스 로딩 정책을 이용하므로 `fetch`보다 우회가 훨씬 어려움.
    2. **검증 로직 최적화**: `Promise.all`을 사용하여 여러 도메인에 대한 이미지 로드 테스트를 병렬로 수행, 감지 속도와 정확도 향상.
    3. **버전 상향**: `v0.14` -> `v0.15` 로 업데이트.
- **결과**: [테스트 필요]

---

### 2026-05-06: 버전 0.2.0 상향, 롤백 백업 및 PGS 자막 OCR 기본 구조 구현
- **문제점**: 
  1. 이미지 자막(PGS) 추출을 위한 구조적인 파이프라인이 부재하여 시간 정보 및 텍스트 변환이 불가능함.
  2. OCR 처리에 대비한 포터블(Portable) 배포 구조 확립이 필요함.
- **수정 과정 및 핵심 코드**:
  1. **사전 백업 및 버전 상향**: `tools/tauri_extractor` 폴더 전체를 `backup/20260506182032` 경로로 복사하여 롤백 지점을 확보. `tauri.conf.json`과 `package.json` 버전을 `0.2.0`으로 상향.
  2. **의존성 추가**: `src-tauri/Cargo.toml`에 고유 ID 생성을 위해 `uuid` 크레이트 추가.
  3. **Rust 백엔드 뼈대 구축**: `lib.rs`에 `extract_pgs_subtitle` 커맨드를 구현 완료.
     - FFmpeg를 이용하여 비디오에서 `.sup` 원본 파일을 추출.
     - OS 임시 폴더에 고유 UUID 작업 폴더를 생성하고 `.sup`에서 개별 PNG 프레임 이미지를 추출.
     - 포터블 폴더(`resources/tesseract/tesseract.exe`)에 위치한 Tesseract 엔진을 동적으로 감지하여 백그라운드 프로세스로 병렬/순차 실행하도록 구조화.
     - 각 프레임 처리 시마다 프런트엔드에 `10% ~ 95%` 범위로 정밀하게 진행률과 안내 메시지를 `emit` 하여 UI 프리징 방지.
     - 작업 완료 후 임시 이미지 파일들과 작업 폴더를 완전히 삭제하는 자동 클린업(Clean-up) 로직 적용.
  4. **프런트엔드 UI 연결 (React)**: `App.tsx` 수정 완료.
     - 사용자가 선택한 트랙 중에 PGS(`pgs`) 코덱이 포함되어 있는지 감지하는 `hasPgsSelected` 상태 추가.
     - PGS가 선택된 경우에만 동적으로 렌더링되는 **[OCR 언어 선택 콤보박스 (한국어/영어)]** UI 추가.
     - 기존의 `extract_subtitle` 호출 로직을 분기 처리하여, 텍스트 자막이 아니면서 코덱이 PGS인 경우 `extract_pgs_subtitle` 커맨드를 호출하고 `language` 파라미터를 넘기도록 구현.
     - 추출 시 파일 확장자를 `.sup`에서 `.srt`로 강제 변환하도록 로직 업데이트.
- **결과**: [성공]

---

### 2026-05-06: PGS OCR 파이프라인 안정화 및 FFmpeg 오류 해결 (v0.2.1)
- **문제점**: FFmpeg `-f sup` muxer 사용 시 MKV 소스에 따라 `incorrect codec parameters` 오류가 발생하며 추출이 중단되는 현상 발생. 또한 수동으로 설정한 타임라인이 실제 영상과 맞지 않는 문제.
- **수정 과정 및 핵심 코드**:
    1. **중간 파일 제거**: `.sup` 파일을 따로 추출하지 않고, 원본 MKV에서 `ffmpeg -filter_complex`를 사용하여 직접 PNG 프레임을 추출하도록 변경.
    2. **정밀 타임라인**: `ffprobe -show_packets`를 사용하여 원본 스트림의 정확한 `pts_time`과 `duration_time`을 JSON으로 획득하여 SRT 생성에 활용.
    3. **비정상 프레임 필터링**: 패킷 크기가 너무 작은(100바이트 미만) 제어용 패킷은 무시하고, 실제 이미지 데이터가 포함된 패킷만 OCR 대상 프레임과 매칭.
    4. **비동기 최적화**: 모든 FFmpeg/ffprobe 호출을 `tokio::process::Command`를 사용한 비동기 방식으로 전환하여 UI 프리징 방지.
- **결과**: [테스트 필요] (FFmpeg muxer 의존성을 제거하여 대부분의 MKV에서 정상 동작 기대)

### 2026-05-06: PGS 자막 추출 실패(0바이트) 수정 및 로그 시스템 도입
- **문제점**: 
    - 일부 MKV 파일에서 PGS 자막 추출 시 최종 `.srt` 파일이 0바이트로 생성되는 현상 발생.
    - 원인 분석 결과, `filter_complex`와 `scale` 필터를 PGS 자막 스트림에 직접 적용할 때 FFmpeg가 실제 자막 그래픽을 렌더링하지 못하고 검은색(빈) 이미지만 추출함 (10KB PNG 파일들 확인).
    - 추출 실패 시 원인을 파악할 수 있는 로그 기록 부재.
- **수정 과정 및 핵심 코드**: 
    - `lib.rs`: PGS 자막의 투명 배경이 OCR 인식을 방해하는 문제를 해결하기 위해 `format=rgb24` 필터를 추가하여 배경을 검은색으로 평탄화.
    - `lib.rs`: FFmpeg의 `passthrough` 모드가 출력하는 모든 프레임(빈 화면 포함)과 `ffprobe` 패킷을 1:1로 매칭하기 위해 패킷 크기 기반 스킵 로직 제거.
    - `lib.rs`: FFmpeg 빌드(8.1-essentials) 환경에 맞춰 `scale` 필터와 분석 옵션(`analyzeduration`, `probesize`)을 최적화하여 적용.
    - `lib.rs`: `log_to_file` 헬퍼 함수를 통해 상세 실행 로그 및 에러 내역을 AppData 경로에 보관.
    - `lib.rs`: FFmpeg/ffprobe 실패 시 `stderr`를 로그에 기록하여 사후 디버깅 가능하도록 보완.
    - `Cargo.toml`: 로그 시간 기록을 위해 `chrono` 의존성 추가.
- **결과**: [테스트 필요]

### 2026-05-06: PGS 자막 투명 배경 문제 및 OCR 인식 실패(빈 문자열) 해결 / 편의성 개선
- **문제점**:
    1. FFmpeg에서 `format=rgb24`를 사용해 PGS를 추출할 때 배경과 글씨색이 명확히 대비되지 않아(특히 투명 배경이 검은색으로 변환되면서 안티앨리어싱 경계가 뭉개짐) Tesseract OCR이 글자를 인식하지 못하고 빈 문자열을 반환(0바이트 SRT 생성)함.
    2. 매번 자막 언어를 수동으로 선택해야 하는 불편함 및 작업 완료 후 초기화(새로운 작업 시작) 기능 부재.
- **수정 과정 및 핵심 코드**:
    1. **OCR 인식률 극대화 (`lib.rs`)**: FFmpeg 추출 필터를 `[0:x]format=rgba,alphaextract`로 변경. PGS의 텍스트 영역(불투명도)을 정확히 추출하여 글씨는 흰색, 배경은 완벽한 검은색으로 반전/평탄화시켜 Tesseract의 인식률을 극대화함.
    2. **자동 언어 선택 (`App.tsx`)**: `useEffect`를 도입하여 사용자가 PGS 트랙을 선택하면 `tags.language` 메타데이터(eng, kor 등)를 파싱하여 OCR 언어 콤보박스를 자동으로 '영어' 또는 '한국어'로 스위칭하도록 구현.
    3. **초기화 버튼 할당 (`App.tsx`)**: 자막 추출이 완료(`isDone`)되었을 때, 하단 추출 버튼을 🔄 '새로운 작업 시작 (초기화)' 버튼으로 상태 전환되도록 렌더링. 클릭 시 앱 상태(`videoPath`, `tracks` 등)를 클리어하여 사용성을 높임.
    4. **수정 전 백업**: `backup/20260506204300` 폴더에 수정 파일 복사 완료.
- **결과**: [테스트 필요]

---

### 2026-05-09: 설정 내 오픈소스 라이선스 정보 조회 기능 추가
- **문제점**: Tesseract OCR, FFmpeg 등 오픈소스 사용 시 필요한 라이선스 고지 의무(Attribution)를 준수하기 위한 UI 부재.
- **수정 과정 및 핵심 코드**:
    1. **다국어 지원 (`i18n.ts`)**: `btn_license`, `license_title`, `license_close` 등 라이선스 전용 번역 키 추가 및 5개국어 대응.
    2. **설정 모달 확장 (`App.tsx`)**: 환경 설정 창 내부에 '라이선스 정보' 링크 버튼 추가.
    3. **라이선스 팝업 구현 (`App.tsx`)**: 별도의 모달 상태(`showLicenseModal`)를 신설하여 Tesseract, FFmpeg, Tauri, React의 라이선스 전문을 보여주는 스크롤 가능한 UI 구현.
    4. **스타일 최적화 (`App.css`)**: 라이선스 텍스트 가독성을 위해 모노스페이스 폰트, 반투명 배경 및 정밀한 모달 레이아웃 적용.
    5. **UI 위치 조정**: 사용자 요청에 따라 '라이선스 정보' 링크를 '홈페이지 이동' 버튼 하단으로 배치하고, 폰트 크기를 `0.75rem`으로 축소하여 시각적 우선순위를 조정함.
- **결과**: [성공]

---

### 2026-05-09: SubFast Extractor 배포용 Release 빌드 수행
- **작업 내용**: Tauri 기반 자막 추출기 애플리케이션의 배포용 실행 파일 및 설치 프로그램 생성.
- **빌드 환경**: Release 모드 (Optimized)
- **결과물 위치**:
    - **설치형 (NSIS)**: `tools/tauri_extractor/src-tauri/target/release/bundle/nsis/SubFast Extractor_0.2.0_x64-setup.exe`
    - **설치형 (MSI)**: `tools/tauri_extractor/src-tauri/target/release/bundle/msi/SubFast Extractor_0.2.0_x64_en-US.msi`
    - **실행형 (EXE)**: `tools/tauri_extractor/src-tauri/target/release/toolstauri_extractor.exe`
- **최적화**: `.pdb`, `.d` 파일 및 `incremental` 폴더 등 빌드 중간 부산물을 제거하여 배포 용량 최적화 완료.
- **결과**: [성공]

---

### 2026-05-09: 자막 추출 즉각 중단 기능 구현 및 UI 오표기 해결
- **문제점**: 
    1. 자막 추출 중 '중단' 버튼을 눌러도 `ffprobe` 단계(최대 10초)에서 지연이 발생하고 즉시 멈추지 않음.
    2. 중단 요청 후에도 UI에 "영상을 분석 중입니다..." 메시지가 다시 나타나 중단이 안 된 것처럼 오해 유발.
    3. 중단 후 재시작 시 이전 프로세스의 잔여 이벤트가 수신되어 진행률이 튀는 현상(이어하기처럼 보이는 착시) 발생.
- **수정 과정 및 핵심 코드**:
    1. **전 과정 PID 추적 및 대기 (`lib.rs`)**: `ffprobe`, `ffmpeg`, `tesseract` 모든 외부 프로세스 실행 시 PID를 `AppState`에 즉시 등록. `stop_extraction` 호출 시 `taskkill` 완료를 기다리도록(`output().await`) 수정하여 확실한 프로세스 종료 보장.
    2. **반응성 극대화**: 모든 프로세스 `spawn` 직전/직후 및 로그 리더 루프 내부에 `cancelled` 플래그 체크를 추가하여 레이스 컨디션 차단.
    3. **이벤트 필터링 (`App.tsx`)**: 프런트엔드 이벤트 리스너에 `isCancelled.current` 체크를 추가하여 중단 이후 도착하는 지연 이벤트를 UI에서 무시.
    4. **클린업 강화**: 중단 시 UUID 기반 임시 디렉토리를 즉시 삭제하여 시스템 자원 낭비 방지.
- **결과**: [성공]

---

### 2026-05-09: [UI/UX] 자막 싱크 결과 화면 가독성 및 다운로드 접근성 개선
- **문제점**: 
    1. 자막 싱크 완료 후 '결과 다운로드' 버튼이 화면 하단에 위치하여 스크롤 없이는 인지하기 어려움.
    2. 동기화 결과 섹션의 너비가 너무 넓어, 대화면에서 고정된 사이드 배너 광고 영역과 겹치는 현상 발생.
- **수정 과정 및 핵심 코드**:
    1. **다운로드 버튼 상단 배치 (`App.tsx`)**: 싱크 작업 버튼 영역(`action-group`)에 동적으로 다운로드 버튼을 추가. 싱크 완료 즉시 스크롤 없이도 결과물을 다운로드할 수 있도록 UX 개선.
    2. **결과 영역 너비 최적화 (`App.css`)**: `.results-section`의 `max-width`를 `1000px`로 제한하고 중앙 정렬 처리. 이를 통해 사이드바 광고(1550px 이상 노출)와의 물리적 간격을 확보하여 겹침 문제 해결.
    3. **시각적 강조**: 다운로드 버튼에 `var(--accent)` (에메랄드 그린) 컬러와 강조 그림자를 적용하여 주요 액션임을 명확히 함.
- **결과**: [성공]

---

### 2026-05-09: [광고/수익화] 좌우 사이드바 광고 플랫폼 서버 동적 제어 시스템 구축
- **문제점**: 좌측(쿠팡), 우측(클릭몬) 광고 플랫폼이 코드에 고정(Hard-coded)되어 있어, 특정 플랫폼의 이슈 발생 시 즉각적인 대응(플랫폼 교체 등)이 불가능함.
- **수정 과정 및 핵심 코드**:
    1. **상태 관리 및 동적 렌더링 (`App.tsx`)**: `leftPlatform`, `rightPlatform`, `mobilePlatform` 상태를 신설하고, `AdSidebar`와 `MobileAdBanner` 컴포넌트가 `platform` 프롭을 받아 조건부 렌더링하도록 구조 개선.
    2. **서버 연동 고도화 (`fetchAdConfig`)**: 파이어베이스의 `web_banners.json` 설정 파일에서 `left_platform`, `right_platform`, `mobile_platform` 값을 읽어와 실시간으로 반영하도록 로직 업데이트.
    3. **안정성 확보 (Fallback)**: 서버 점검이나 네트워크 오류 시에는 기존의 기본값(좌: 쿠팡, 우: 클릭몬)으로 자동 작동하도록 방어 코드 적용.
- **결과**: [성공]

### 2026-05-09: [광고 차단기 감지 완화] 팝업 광고용 브릿지 페이지 도입
- **문제점**: 메인 브라우저에서 광고 차단기를 비활성화했음에도 불구하고, 싱크 맞추기 클릭 시 새 창(광고 도메인)으로 뜨는 광고가 차단기에 의해 감지되어 진행이 막히는 문제 발생.
- **수정 과정 및 핵심 코드**:
    1. **브릿지 페이지 생성 (rontend/public/ad-bridge.html)**: 타 도메인의 광고 링크를 직접 띄우지 않고, 현재 서비스와 동일한 도메인을 사용하는 브릿지 페이지를 생성.
    2. **사용자 가이드 제공**: 브릿지 페이지 내부에 로딩 스피너 및 안내 문구를 포함하고, 1.5초 후 타겟 광고 URL로 리다이렉트하거나 스크립트(클릭몬)를 주입하도록 구현. 광고가 안뜰 경우 새 창에서도 차단기를 꺼야 한다는 경고 문구 추가.
    3. **팝업 로직 수정 (rontend/src/App.tsx)**: window.open 실행 시 광고 링크 대신 /ad-bridge.html?target=... 혹은 ?type=clickmon을 호출하도록 변경하여 초기 팝업 차단 및 확장 프로그램 감지 우회 유도.
- **결과**: [테스트 필요]

### 2026-05-09: [보안/UX] API 토큰 안내 강화, 좌우 광고 중복 방지 및 v0.16 업데이트
- **문제점**: 
    1. 사용자가 API 키를 입력하지 않아 매칭이 실패할 경우, 기술적인 이유(매칭률 저하)로만 안내되어 사용자가 원인(API 키 부재)을 파악하기 어려움.
    2. 좌우 사이드바 광고가 랜덤으로 선택될 때 동일한 광고 ID가 양쪽에 노출되어 시각적 단조로움 및 광고 효율 저하 발생.
- **수정 과정 및 핵심 코드**:
    1. **에러 메시지 정밀화 (`backend/aligner.py`)**: 매칭률이 30% 미만일 때 `api_key` 유무를 체크하여, 키가 없을 경우 "API 토큰(키)이 없거나 부족하여 정밀 매칭에 실패했습니다."라는 명확한 안내 문구로 분기 처리.
    2. **광고 중복 방지 로직 (`App.tsx`)**: `useState` 초기값 설정 및 서버 설정 로드 시 `while` 루프를 사용하여 `leftBannerId`와 `rightBannerId`가 서로 다른 값이 되도록 보장.
    3. **버전 업데이트**: `App.tsx` 푸터 및 `package.json` 버전을 `v0.16` (0.16.0)으로 상향.
- **결과**: [테스트 필요]
### 2026-05-09: 불필요한 버튼 클릭 추적(trackClick) 로직 삭제
- **문제점**: 토큰 사용량이 건당으로 이미 상세히 기록되고 있어, 단순 버튼 클릭 수집은 데이터 중복 및 불필요한 네트워크 요청을 발생시킴.
- **수정 과정 및 핵심 코드**:
    1. `frontend/src/App.tsx`: `trackClick` 함수 정의부 삭제.
    2. `handleSync`, `handleDownloadApp` 내의 `trackClick` 호출부 제거.
- **결과**: [성공] (수정 전 `backup/20260509040400`에 백업 완료)

---

### 2026-05-09: [광고 차단기 감지] 오작동 로직 수정 및 컴포넌트 생명주기 대응
- **문제점**: 
    1. 지능형 차단기가 로컬 파일(`ads.js`)을 허용하면 나머지 실제 광고 도메인 체크를 건너뛰어 감지를 우회하는 현상 발생.
    2. `App.tsx` 로드 시 `isProduction` 상태 업데이트가 비동기로 처리됨에 따라 `checkAdBlocker`가 항상 `false`로 초기화되어 페이지 초기 로드 시 차단기 감지 자체가 실행되지 않음.
- **수정 과정 및 핵심 코드**:
    1. `checkAdBlocker` 로직 재작성: 로컬 파일 허용 여부와 무관하게 이미지(`Image`) 및 네트워크(`fetch`) 차단 여부를 모두 수집한 후, 하나라도 차단 신호가 있으면 감지 상태를 `true`로 설정하도록 종합 판별 로직 적용.
    2. React 생명주기 수정: 비동기 설정 로딩 이후 `isProduction` 상태가 `true`로 변경될 때 비로소 `checkAdBlocker`가 실행되도록 `useEffect` 의존성 배열(`[isProduction]`) 추가.
- **결과**: [성공]

### 2026-05-09: 자막 추출기 포터블(Portable) 배포 빌드 및 자동화 스크립트 구축
- **문제점**: 배포 시 설치 프로그램(MSI/NSIS) 없이 실행 파일과 필수 리소스(FFmpeg, Tesseract)만으로 구성된 독립 실행형(Portable) 패키지 필요. 개인정보 및 핵심 소스 코드 유출 방지(Security by Design)를 위한 빌드 설정 최적화 요구.
- **수정 과정 및 핵심 코드**:
    1. **보안 및 최적화 설정 (`Cargo.toml`)**: `[profile.release]`에 `strip = true` (심볼 제거), `lto = true`, `opt-level = "z"`를 적용하여 리버스 엔지니어링 방지 및 파일 크기 최소화.
    2. **자동화 스크립트 (`build_portable.ps1`)**: Tauri 빌드, 실행 파일 추출, 필수 리소스(Tesseract), 외부 바이너리(FFmpeg)를 하나의 `dist_portable` 폴더로 수집하는 원클릭 파워쉘 스크립트 구현.
    3. **빌드 정책 수립 (`tauri_build_policy.md`)**: 향후 동일한 방식으로 빌드할 수 있도록 보안 지침 및 폴더 구조를 포함한 정식 빌드 규칙 문서를 `.agent/rules`에 추가.
- **결과**: [테스트 필요] (빌드 자동화 도구 및 정책 수립 완료)

### 2026-05-09: 배너 광고 테스트 모드 적용 (로컬 설정 시뮬레이션)
- **문제점**: 서버 설정 변경 없이 로컬 환경에서 특정 배너 이미지와 타겟 URL(네이버) 노출 테스트 필요.
- **수정 과정 및 핵심 코드**:
  1. **테스트 이미지 생성**: `generate_image` 툴을 사용하여 가로형 테스트 배너 이미지를 생성하고 `public/test_banner.png`로 저장.
  2. **로컬 설정 파일 생성**: `public/test_config.json`을 생성하여 배너 활성화 상태, 이미지 경로, 타겟 URL(`https://www.naver.com`) 정의.
  3. **CONFIG_URL 스위칭**: `App.tsx`의 `CONFIG_URL`을 파이어베이스 원격 주소에서 로컬 `/test_config.json`으로 일시 변경.
- **결과**: [성공] (네이버 링크 이동 및 배너 노출 검증 완료, 테스트 후 원격 설정으로 복구 완료)

### 2026-05-09: 킬 스위치(Kill Switch) 작동 테스트 및 링크 개선
- **문제점**: 긴급 점검 또는 강제 업데이트 필요 시 버전 차단 기능 작동 여부 확인 및 업데이트 버튼의 신뢰성 확보 필요.
- **수정 과정**:
  1. `test_config.json`의 `min_version`을 현재 앱 버전("0.2.0")보다 높은 "1.0.0"으로 설정하여 차단 로직 시뮬레이션.
  2. 업데이트 버튼 클릭 시 `window.open` 대신 Tauri `openUrl`을 사용하도록 `App.tsx` 로직 개선.
- **결과**: [성공] (0.2.0 버전 차단 확인 및 업데이트 버튼 동작 개선 완료, 테스트 후 원격 설정으로 복구 완료)

---

### 2026-05-09: 자막 추출기(SubFast Extractor) 정식 배포용 포터블 빌드
- **작업 내용**: 사용자 요청에 따른 배포용 정식 빌드 수행. `build_portable.ps1` 자동화 스크립트를 사용하여 독립 실행형 패키지 구성.
- **빌드 환경**: 
  - **모드**: Release (최적화 및 심볼 제거 적용)
  - **버전**: v0.16.0 (tauri.conf.json 기준)
  - **대상**: Windows (x64)
- **수정 과정 및 핵심 코드**:
  1. **사전 백업**: `backup/20260509052549` 폴더에 소스 코드 백업 완료.
  2. **자동 빌드**: `npm run tauri build`를 통한 최적화된 바이너리 생성.
  3. **패키징**: `dist_portable` 폴더에 EXE, FFmpeg, Tesseract 리소스 수집 및 불필요한 .pdb 파일 제거.
- **결과**: [성공] (05:31 빌드 완료 및 `dist_portable` 패키징 확인)

---

### 2026-05-09: 추출기 하단 배너 테스트 완료 및 설정 복구
- **작업 내용**: 네이버 링크 및 테스트 배너 노출 확인 후, 기존 'FlowStateTimer' 설정으로 원복 수행.
- **수정 과정**:
  1. `server/public/latest_version.json`: `ad_id`, `image_url`, `html_url`, `target_url`을 기존 운영 값으로 복구.
  2. **CI 배포**: `.env` 내의 `FIREBASE_TOKEN`을 사용하여 실서버 배포 완료.
  3. **규칙 수립**: 향후 동일한 수정을 한 번에 안전하게 수행할 수 있도록 `.agent/rules/banner_remote_config_policy.md` 정책 문서 신설.
- **결과**: [성공] (실서버 원복 완료 및 정책 수립 완료)

---

### 2026-05-09: v0.2.0 버전 킬 스위치(Kill Switch) 해제 및 설정 복구
- **작업 내용**: 킬 스위치 테스트 완료 후, 구버전(0.2.0) 사용자 차단 해제 및 정상 작동 모드로 복구.
- **수정 과정**:
  1. `server/public/latest_version.json`: `min_version`을 `0.01`로 원복.
  2. **안내 메시지**: 업데이트 필요 알림 비활성화 및 시스템 점검 완료 공지로 교체.
  3. **배포**: CI 토큰을 이용해 실서버 배포 완료.
- **결과**: [성공] (0.2 버전 앱 정상 실행 및 기능 복구 확인)

---

### 2026-05-09: 자막 추출기(SubFast Extractor) v2.0 다운로드 링크 업데이트
- **문제점**: 홈페이지의 자막 추출기 다운로드 링크가 구버전(.exe)으로 고정되어 있어 최신 v2.0(.zip) 패키지를 제공하지 못함.
- **수정 과정 및 핵심 코드**:
    - `frontend/src/App.tsx`: `executeActualDownload` 함수 내 `downloadUrl`을 새로운 드롭박스 링크(`https://www.dropbox.com/scl/fi/dzebaz75prvdj9q5sqkqr/v2.0.zip?rlkey=dmd2vgdd26itv7wu9pr3n7xd7&st=t5kzqlq3&dl=1`)로 변경.
- **결과**: [테스트 필요]

---

### 2026-05-09: 브라우저 서버 통합 배포 및 정책 업데이트
- **문제점**: 브라우저 서버(프론트엔드, 백엔드, 관리 서버 등) 통합 배포 절차의 명문화 및 실행 필요.
- **수정 과정 및 핵심 코드**:
  - `.agent/rules/deployment_policy.md`에 '브라우저 서버 통합 배포' 규칙 추가.
  - 프로젝트 전체 백업 수행 (`backup/202605090548`).
  - GitHub (`master` branch) 푸시를 통한 백엔드(Render) 및 프론트엔드(Vercel) 배포 트리거.
  - Firebase Hosting을 통한 정적 설정 및 관리 도구 배포 (`server` 디렉토리).
- **결과**: [테스트 필요]

---

### 2026-05-09: 광고 차단기 오탐 해결 및 베이트 엘리먼트 방식 도입 (v0.17)
- **문제점**: 광고 차단기를 꺼도 감지 경고가 사라지지 않는 현상 발생. 원인은 감지용 외부 리소스(Clickmon 등)가 서버 장애(404)를 일으켜 이를 차단으로 오인한 것이었음.
- **수정 과정 및 핵심 코드**:
    1. **베이트 엘리먼트(Bait Element) 도입**: 외부 네트워크에 의존하지 않고, 페이지 내에 광고 차단기가 감지할 만한 클래스명을 가진 숨겨진 `div`를 추가하여 상태를 체크하는 방식 도입.
    2. **과반수 원칙(Majority Rule)**: 로컬 스크립트(`ads.js`), 베이트 엘리먼트(DOM), 구글 패비콘(Image) 3가지 중 2가지 이상이 감지될 때만 최종 차단으로 판정 (2/3 원칙).
    3. **정밀 검증**: 가상 브라우저 및 로컬 개발 서버(`http://localhost:5173`)를 통해 실제 광고 노출 중에도 경고창이 뜨지 않음을 최종 확인.
- **결과**: **[테스트 필요]** (로컬 검증 완료, 운영 서버 배포 대기 중)

### 2026-05-09: 광고 차단 감지 로직 강화 및 테스트 모드(?adblock_test) 도입
- **문제점**: 광고 차단기가 켜져 있음에도 불구하고, 로컬 스크립트가 차단되지 않는 등의 이유로 감지 시스템이 정상 작동하지 않는 현상 보고됨.
- **수정 과정 및 핵심 코드**: 
    1. `frontend/src/App.tsx` 내 `checkAdBlocker` 로직 강화: 기존 복합 조건(`domBlocked || (localBlocked && imagesBlocked)`)을 단순 OR 조건(`domBlocked || localBlocked || imagesBlocked`)으로 변경하여 감지 민감도 극대화.
    2. 테스트 모드 추가: URL 파라미터 `?adblock_test=1` 접속 시 무조건 차단 경고 노출, `?adblock_test=0` 접속 시 무조건 허용 처리하여 뷰어 및 내부 테스트 편의성 확보.
    3. 실행 환경 확장: `isProduction`이 `false`인 개발 환경에서도 테스트 파라미터가 감지되면 광고 차단 로직이 즉시 실행되도록 `useEffect` 의존성 및 조건문 개선.
- **결과**: [성공] (뷰어 테스트를 통해 온오프 기능 및 강화된 감지 로직 정상 작동 확인 완료)

### 2026-05-09: 줄바꿈 및 한글 가독성 최적화 (word-break: keep-all)
- **문제점**: 한글 단어가 중간에 잘린 채 줄바꿈되는 현상으로 인해 가독성이 저하되고, 긴 영문이나 링크가 영역을 벗어나는 현상 방지 필요.
- **수정 과정 및 핵심 코드**:
  1. **사전 백업**: `backup/20260509065254` 외 4개 폴더에 관련 파일 백업 완료.
  2. **전역 스타일 적용**: `frontend/src/index.css`, `tools/tauri_extractor/public/banner/index.html`, `img/flowstatetimer/index.html`의 `body` 태그에 `word-break: keep-all;` 속성을 강제 적용하여 한글 단어 단위 줄바꿈 보장.
  3. **넘침 방지 유틸리티**: `index.css`에 `.break-words` 클래스(`word-break: break-word; overflow-wrap: break-word;`)를 추가하여 Tailwind 방식의 예외 처리 지원.
  4. **컴포넌트 반영**: `App.tsx` 내의 광고 차단 경고, 안내 가이드, 개인정보처리방침, 면책조항 등 텍스트가 많은 요소들에 `break-words` 클래스를 적용하여 긴 영문/링크 대응.
- **결과**: [테스트 필요]

---

### 2026-05-09: [배포] 브라우저 서버 통합 배포 (v0.17)
- **문제점**: UI 가독성 개선, 광고 차단기 감지 로직 강화 및 테스트 모드 도입에 따른 실서버 반영 필요.
- **수정 과정 및 핵심 코드**:
  1. **사전 백업**: `backup/20260509071055` 폴더에 관련 소스 코드 전체 백업 완료.
  2. **통합 배포 수행**:
     - **GitHub Push**: `master` 브랜치 푸시를 통해 Cloudflare Pages(프론트엔드) 및 Render(백엔드) 자동 배포 트리거.
     - **Firebase Deploy**: `server` 디렉토리 내 정적 리소스 및 매니저 설정을 Firebase Hosting(`subfast-manager`)에 수동 배포 완료.
  3. **버전 정보**: v0.17.0 (Frontend/Backend) 및 0.2.0 (Extractor) 설정값 동기화.
- **결과**: [성공] (실서버 반영 완료: https://mainko.net)

---

### 2026-05-09: 광고 팝업 실시간 검증(Heartbeat) 및 차단 감지 강화
- **문제점**: 메인 창에서 광고 차단기를 껐더라도, 새로 열리는 광고 팝업 창에서 다시 차단되거나(AdGuard 등), 광고 스크립트가 실행되지 않은 채로 싱크가 시작되는 '수익 손실' 및 '사용자 경험 불일치' 문제 발생.
- **수정 과정 및 핵심 코드**:
    1. **양방향 통신 도입 (postMessage)**: `ad-bridge.html`(팝업)이 로드되고 1.5초간 유지되었을 때 메인 창으로 `AD_BRIDGE_ACTIVE` 신호를 보내도록 개선.
    2. **팝업 상태 추적**: `App.tsx`에서 `isAdPopupActive` 상태와 `useRef`를 통해 팝업의 생존 여부를 실시간으로 추적.
    3. **네트워크 프로브(Network Probe) 도입**: `ad-bridge.html` 내에서 리다이렉트 직전에 `fetch('https://omg10.com/favicon.ico')`를 시도하여 광고 도메인 가용성을 테스트.
    4. **이중 검증 로직**: 싱크 시작 전 2.5초 대기 후, 기존 `checkAdBlocker()` 결과뿐만 아니라 팝업으로부터의 활성 신호(`isAdPopupActive`)가 왔는지까지 검사하도록 강화.
- **결과**: [테스트 필요]

---


